[package]
name = "sv-sys-sodium"
description = "FFI to libsodium (via libsodium-sys-stable): crypto_sign, generichash, secretbox — safe wrappers."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
libsodium-sys-stable = "1"

[lints]
workspace = true
