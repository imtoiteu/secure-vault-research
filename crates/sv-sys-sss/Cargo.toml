[package]
name = "sv-sys-sss"
description = "FFI to Daan Sprenkels' sss hazmat key-sharing (vendored hazmat.c). randombytes supplied from Rust."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true
links = "sss_hazmat"
build = "build.rs"

[dependencies]
sv-crypto-traits = { path = "../sv-crypto-traits" }
getrandom = "0.2"

[build-dependencies]
cc = "1"

[lints]
workspace = true
