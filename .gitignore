/target
# the workspace-excluded desktop/ app has its OWN target (multi-GB) — never commit it
/desktop/target
/.bin
# bundled, hash-pinned age binaries are placed at build/release time, never committed
/desktop/binaries/age
/desktop/binaries/age-keygen
**/*.rs.bk
Cargo.lock.orig
# SBOM / audit artifacts produced by CI
/sbom*.txt
/sbom*.json
# Local env
.DS_Store

# Thư mục nạp sẵn của Python trong bộ sinh hồ sơ — sinh lại được, không cần theo dõi
hoso-sang-kien/__pycache__/

# Thư mục làm việc trung gian của chup-giao-dien.py — sinh lại được từ app/frontend
hoso-sang-kien/hinh-anh/screenshot/_work/
# Tệp mẫu do đơn vị cung cấp để tham chiếu thể loại, không thuộc bộ hồ sơ nộp.
hoso-sang-kien/DCSB_HV2_restructured2.docx
